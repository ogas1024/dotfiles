# ============================================================================#
# 通用编辑器 / 语言环境设置（与 XDG 目录无强耦合的环境变量）
# - 本文件主要放：
#   * 默认编辑器（EDITOR / SUDO_EDITOR）；
#   * 将来若需要的语言 / 区域设置（LANG / LC_*）等。
# - 与各工具的“XDG 化”无关的环境变量建议放在这里。
# ============================================================================#

# 默认编辑器：按个人习惯选择 nvim/micro/vim 等。
export EDITOR="nvim"
export SUDO_EDITOR="nvim"

# 如有需要，可以在此设置语言/编码：
#   export LANG="zh_CN.UTF-8"
#   export LC_CTYPE="zh_CN.UTF-8"

