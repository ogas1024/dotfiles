# ============================================================================#
# 杂项小函数
# - 将不属于特定模块的通用小工具集中在此处，便于复用与维护。
# ============================================================================#

# 临时目录：一键进入一个新的 /tmp 目录，退出后自动清理由系统处理。
tmp() {
  cd "$(mktemp -d /tmp/temp_XXX)"
}

